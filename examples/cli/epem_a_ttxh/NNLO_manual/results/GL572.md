# GL572 threshold/IR audit

## Provenance and topology

- Raw diagnostic DOT SHA-256: `9119a3f9ac1b1c83d04147d71e1cfb49096423eb7b178baa43995af5c27fc343`.
- Clean standalone DOT SHA-256: `61deba8aa0ef0a7f0969d5e7d11654c5a9c27a721c7158c50a754331bf833ad9`.
- Final run-card SHA-256: `ec63b3569f27a4573d93fabcbf60fcbbe3a2824830ddbc1d840920b84f5685b1`.
- Pinned CLI SHA-256: `fc9d20e0ffbca1b334ab57908b29aeea34ecda68f5c3a76664d8ca4f8536256c`.

The clean DOT preserves every edge, particle, interaction, and the four
`lmb_id` basis edges while removing only autogenerated routing/drawing fields.
GL572 has no `V_36` or `V_37`, so no hard-collinear sector belongs to its
mandatory queue. With `a=K0`, `b=K1`, `c=K2`, `d=K3`, and zero incoming
spatial momentum sum, the raw routing is

```text
e2  = b-a             H
e3  = a               t, lmb_id 0
e4  = b               t, lmb_id 1
e5  = b+c-a           t
e6  = c               t, lmb_id 2
e7  = d               t, lmb_id 3
e8  = d               t
e10 = b+c-a           t
e11 = P0+P1           a, fixed spatial momentum 0
e12 = a+d-c           t
e13 = a-c             g = q13
e14 = d-c             g = q14
```

Thus q13 and q14 are independent rank-3 soft constraints and their common
locus has rank 6. The final card contains explicit 50-point, signed,
logarithmic complete-graph blocks for q13 soft, q14 soft, and their independent
double-soft limit, plus `all_approaches`. Their candidate coordinates are

| path (rank) | midpoint `(a;b;c;d)` | axis `(a;b;c;d)` | status |
|---|---|---|---|
| q13 soft (3) | `(74,-52,137;-42,39,157;74,-52,137;-91,64,83)` | `(211,-157,193;0;0;0)` | unrun |
| q14 soft (3) | `(123,-77,211;37,126,-109;74,-52,137;74,-52,137)` | `(0;0;0;98,-86,342)` | unrun |
| q13+q14 soft (6) | `(74,-52,137;37,126,-109;74,-52,137;74,-52,137)` | `(211,-157,193;0;0;98,-86,342)` | unrun |

These points could not be checked against generated E-surfaces. They remain
reproducible candidate paths rather than evidence and are not assigned a fit
or classification.

## Generation blocker

The exact ttH NNLO process specification was used with no forced cut or
orientation selection, and full local plus integrated UV requested first. The
final preserved card explicitly disables compilation; neither timed-out
attempt reached evaluator save or compilation. The guarded full-UV command remained active until its
600-second stop and did not reach `save state`. Its guard records
`exit_reason=timeout`, return code -15 after cleanup, 632.492162 s wrapper
duration including signal grace, 1,996,259,328 bytes peak recursive RSS, and
111,176,867,840 bytes minimum free disk. The complete original log and guard
record are preserved as `generate_full_uv.log` and
`guard/generate_full_uv.guard.json`.

Following the campaign contract, exactly one clean fallback set
`global.generation.uv.generate_integrated=false` before `generate existing`.
It likewise did not reach `save state` before its 420-second stop. Its guard
records `exit_reason=timeout`, return code -15 after cleanup, 452.513899 s
wrapper duration, 1,825,767,424 bytes peak recursive RSS, and
109,864,288,256 bytes minimum free disk. Its log and guard record are
preserved as `generate_local_only.log` and
`guard/generate_local_only.guard.json`. No generation retry was made.

Because neither attempt saved a generated state, the following evidence is
explicitly unavailable:

- process-valid cut and orientation counts;
- generated and cut-active E-surface inventories;
- correlated soft/threshold intersections and accidental-locus rejection;
- explicit approach JSON, signed-branch fits, and target-distance slopes;
- per-cut threshold-counterterm components and threshold-only checks;
- internal `generation_summary.json` time and evaluator count.

The generation failure is therefore a hard workflow blocker, not an IR pass
or fail. All required generic and correlated limits are recorded as
incomplete, so `all limits OK? = inconclusive`.

## Directive decision

No `threshold_counterterms` directive is accepted. There is no generated
cut/E-surface metadata and no approach evidence from which to identify a
coalescing counterterm, projected parent, or named variant. Disabling threshold
subtraction was never attempted as a cure, and no ambiguous embedding was
guessed. The committed DOT remains clean and unedited scientifically.

## Integration

Integration was not attempted because no full-UV or local-UV-only generated
state exists. Consequently there is no completed checkpoint, central value,
componentwise-absolute value, error, `neval`, internal generation time,
runtime per sample per core, stability percentage, or maximum-weight replay.
`converged? = no` and every numerical integration field is explicitly
unavailable. This is a generation blocker, not a zero result.

## Conclusion

`threshold struct. edited? = no`; `converged? = no`; `all limits OK? =
inconclusive`; `UV mode = blocked`. GL572 exhausted its one full-UV attempt
and one prescribed local-only fallback without producing a state. The routing,
clean DOT, complete setup/generation/integration contract, and three generic
IR candidate blocks are reproducible, while every cut-dependent or numerical
claim is honestly left incomplete. Both guarded failures and descendant
cleanup evidence are retained for review.
