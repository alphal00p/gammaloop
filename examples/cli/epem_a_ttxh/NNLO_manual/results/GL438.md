# GL438 remediation audit

## Final status

- **Threshold structure edited:** no. The final DOT is byte-identical to the
  task-start graph and retains threshold subtraction for every cut and all 504
  orientations.
- **Full UV generation:** yes, from a clean final state with local and
  integrated UV counterterms.
- **Selected random UV profile:** pass under the explicit vanishing-fit
  contract for all 504 orientations and the summed integrand.
- **All limits OK:** yes. Exact Arb adjudication now closes the generic q12,
  isolated q12/two-top, independent double-soft, and both remaining
  cancellation-limited collinear sectors.
- **Integration converged:** yes. Because the DOT is unchanged, the prior
  finite 2,400-sample checkpoint remains the applicable integration result;
  no new integration was run during this remediation.

The former first-pass radial-root blockers were artifacts of degenerate probe
geometry, not evidence that a threshold counterterm needed a new projection.
The audit was therefore redone at generic, nonzero momenta that isolate each
complete same-root family.

## Provenance and clean generation

| artifact | SHA-256 |
|---|---|
| `target/release/gammaloop` | `0f8ed2c842814995bdcad19b3bc2a406eb75cb4a1784a74aa3c3de6b0e9d2b1b` |
| final `graphs/GL438.dot` | `90ee30e2b5ae92db8b78750ff85049e00a8b6961c3635d94b7127e40475e84a8` |
| final `run_cards/run_GL438.toml` | `54b471a7d5ded3e6cfe5a029e3492994a38ec069689d47092660d1ac35d6da94` |
| final `generation_summary.json` | `3c0e0ea492f1c315d2688bb1b16b15f538abab778e57ca7658fa730fbce802c8` |
| selected `uv_profile.json` | `3fa01b6018a14f396bad945802d5dd1f188d1e89ee54b086b406c6b55253038c` |

The final clean state is
`states/state_GL438/trials/final_state`. Full generation completed normally in
403.190 s under the shared watchdog, with 3,533,135,872 bytes peak recursive
RSS and at least 428,999,049,216 bytes free on the monitored disk. The internal
summary reports 24 evaluators, 397.609 s total generation time,
3,678,613,504 bytes peak RAM, 74.391 s in spenso, 268.341 s in Symbolica, and
zero compilation time. The state contains all 504 orientations and eight
process-valid cuts; no cut or orientation was forced.

The first final-wrapper invocation stopped at the read-only-state card
compatibility check because two saved block point counts had been changed; it
launched no physics command. The card was restored to the saved definitions,
and only the subsequent successful/atomically serialized checks are used.

## Routing and threshold families

The four-loop generation basis is `(e2,e4,e7,e12)`, with

```text
q12 = K3
q13 = K1 - K2 - K3
q14 = K2 - K1
q12 + q13 + q14 = 0.
```

Thus a single-soft locus has rank 3, the coincident all-gluon locus has rank 6,
and a hard-collinear transverse locus has rank 2. The eight cuts and their
active left threshold surfaces are unchanged:

| cut | edges | active surfaces |
|---:|---|---|
| 0 | `[2,5,8,14]` | E54, E40 |
| 1 | `[2,5,8,12,13]` | none |
| 2 | `[2,4,5]` | E40, E28, E43, E39, E27, E45 |
| 3 | `[2,3,11,13]` | E54, E40, E28 |
| 4 | `[2,3,11,12,14]` | none |
| 5 | `[2,3,7,14]` | none |
| 6 | `[2,3,7,12,13]` | none |
| 7 | `[2,3,6]` | E40, E28, E51, E22 |

At the three single-soft loci, equal-root surfaces form these complete
families:

- `q12=0`: E40/E28 is the two-top family; E43/E39/E27/E45/E51/E22 is a
  distinct bridge family; E54 is unrelated.
- `q13=0`: E54/E40/E27/E45/E51/E22 is one bridge family; E28, E43, and E39
  are away or separate.
- `q14=0`: E54/E40/E43/E39 is the main bridge; E28/E27/E51 is the two-top
  family; E45/E22 is a separate root.

The corrected run-card paths place every non-target massive momentum at a
generic nonzero value and keep unrelated surface functions away from zero.
Their exact midpoint identities and nonzero first derivatives are encoded in
the card. This removes the simultaneous unrelated roots and top-at-rest
configurations that caused the first-pass center/radial validation failures.

## Causal IR/threshold results

Fits use `|I(lambda)| ~ |lambda|^-p`. A rank-`r` limit is integrable for
`p < r`. F64 envelopes use the nearest 8, 12, and 16 points on both signed
branches. The final Arb checks use either eight or sixteen logarithmic points
per signed branch; all reported JSON points are finite and evaluated.

| limit | rank | precision | threshold on `p` | threshold off `p` | result |
|---|---:|---|---:|---:|---|
| physical e12/e13 collinear | 2 | Arb | `[-9.73e-5, 9.97e-5]` | `[-7.71e-4, 7.92e-4]` | pass: bounded plateaus |
| q12 / isolated complete bridge | 3 | Arb | `[0.966153,0.967779]` | `[0.999346,1.019671]` | pass |
| q13 / isolated complete bridge | 3 | f64 | `[1.997299,2.000766]` | `[0.999978,1.002880]` | pass |
| q14 / isolated main bridge | 3 | f64 | `[1.996646,2.001098]` | `[0.989585,0.999812]` | pass |
| q14 / isolated E22-E45 | 3 | f64 | `[1.998446,2.000323]` | `[0.994412,0.999929]` | pass |

The q12 bridge is the decisive causal comparison. F64 threshold-on totals had
an apparent nearest-eight `p=3.265..3.426`, while threshold-off gave
`p=0.998..1.002`. In Arb, the threshold-on total instead has
`p=0.9662..0.9678` with `R2 >= 0.99898`; its original and aggregate
threshold-CT pieces are individually about `p=1.02`. The apparent f64 failure
was cancellation loss, not a nonintegrable threshold counterterm.

The e12/e13 Arb total is essentially constant. Its low formal fit `R2` is the
expected consequence of fitting a plateau. Individual process-valid cuts 1
and 6 scale as `p=2`, but their all-cut sum cancels to the bounded Arb result.
The generated graph has no explicit component registry, so
`threshold_counterterm_0` is audited as the generated aggregate, with
per-cut original/aggregate event weights retained in each JSON.

The final exact adjudicators close every remaining historical ambiguity:

| sector | rank | exact threshold-on `p` | control / magnitude | JSON SHA-256 | result |
|---|---:|---:|---|---|---|
| generic q12 soft | 3 | `[0.959533,0.999612]`, `R2>=0.997901` | active CT; all 32 points finite | `14f76985cf9726ff82182aa168da00ec9388a9a00492383c46ea3efe633985d8` | pass |
| q12 / isolated E28-E40 two-top | 3 | `[1.015455,1.016155]`, `R2>=0.999775` | off `[1.041486,1.046698]`; both pass | `9b9d352e43f898a4391b9f5a17be454f1dead498957702e9ef753fcc97aa46f0` | pass |
| independent double soft | 6 | `[2.999785,3.002826]`, `R2>=0.99999915` | active CT; all 32 points finite | `29098eefa4978e398641251847b633f31cfab41ca1812f295cdc0381f10c1c7e` | pass |
| physical e12/e14 collinear | 2 | `[-6.593e-5,6.904e-5]` | bounded `2.5009e-37..2.5052e-37`; off is also flat | `6f10b8bc08026d23add2284bd37f645b46d9df8a9eeff3d854ef24d6a1a11238` | pass |
| e13/e14 collinear diagnostic | 2 | `[-1.607e-4,1.667e-4]` | bounded `1.6068e-37..1.6136e-37` | `c19a6909228d09c1c085d992424f3ef22bf3467154c2e3be5e42d71c218d9583` | pass |

The low formal `R2` of the two nearly constant collinear series is the expected
plateau case. The matched e12/e14 threshold-off series is likewise bounded at
`p=[-5.298e-5,5.706e-5]` (SHA-256
`7cb63116b6cd600e1271f318e4efbdfec018a6352f3088ef40d1f0c7e075718f`),
so threshold subtraction is not causally worse. The first aggregate wrapper
was interrupted only after the generic q12, two-top on/off, and double-soft
JSON files had been atomically written; its partial next path is not used. The
separate reduced collinear guard completed normally in `456.025423 s`, peaked
at `6,620,950,528` bytes recursive RSS, and kept at least
`428,923,813,888` bytes free.

No GL638-style one-loop/two-loop partition or GL297-style projected parent is
accepted: those constructions are justified only after a causal threshold-CT
failure is localized. Here the only apparent causal failure disappears in Arb,
while every isolated complete family is integrable with the pristine
association inventory.

## Selected random UV certification

The certification profile is a seeded random, no-fixed-ray run with threshold
subtraction enabled:

```text
seed = 43820260809
graph = 0
LMB = 0 = [e2,e4,e7,e12]
subset mask = 14
fixed = [e2]
free = [e4,e7,e12]
scales = [1e6,1e7,1e8,1e9,1e10]
```

The selector materialized exactly one graph, one LMB, and one subset. All 504
orientation entries have `finite_samples=5`, `positive_finite_samples=0`,
`analysis=null`, and `missing_fit_is_vanishing=true`. The summed entry has the
same counters. The root document has
`allow_vanishing_missing_fits=true`, so all 505 missing fits are accepted as
vanishing; there are zero bad counters and no slope/R2 exceptions to audit.
The guarded run completed normally in 213.780 s, peaked at 3,070,517,248 bytes
recursive RSS, and did not use `--timeout-success`.

Evidence is in
`states/state_GL438/trials/remediation/uv_random_g0_lmb0_mask14_n5_exp6_10_seed43820260809/uv_profile.json`
and `states/state_GL438/trials/guards/final_random_uv_mask14.json`; the profile
SHA-256 is `3fa01b6018a14f396bad945802d5dd1f188d1e89ee54b086b406c6b55253038c`.

## Integration and conclusion

The byte-identical DOT preserves the prior completed 2,400-sample checkpoint:

- signed central value `(+2.802829998758945e-5, -3.974103213952622e-5 i)`
  with errors `(2.9098336399925265e-5, 3.0644833151792247e-5)`;
- componentwise-absolute central value
  `(+9.255865820523395e-5, +6.930805631169967e-5 i)` with errors
  `(2.904254689046601e-5, 3.0622896637485036e-5)`;
- relative absolute errors 31.3775% and 44.1837%; nonfinite and finite-Double
  unstable fractions both 0%.

This satisfies the campaign's accepted absolute-error criterion, so
`converged? = yes`. Maximum-weight coordinates were not replayed in the first
lease, so their IR/threshold classification remains incomplete.

Final conclusion: GL438's pristine threshold prescription survives clean full
UV regeneration, selected per-orientation random UV certification, Arb causal
testing, and corrected nondegenerate threshold-family probes. The prior root
exceptions do not reproduce. No threshold or LMB edit is justified. Every
required physical, correlated-threshold, and conservative diagnostic limit is
now either a stable integrable power or an exact bounded plateau, so
`all limits OK = yes`.
