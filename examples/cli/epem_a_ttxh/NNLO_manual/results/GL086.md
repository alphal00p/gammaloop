# GL086

## Final status

- Lease deadline: 2026-08-09 19:50:37 UTC (`1786305037`).
- Threshold structure edited: **no accepted directive**. The final DOT is the
  pristine no-directive graph; all generated threshold associations remain.
- All required limits OK: **no**. The decisive baseline q12/threshold bridge
  fails, the minimal q12 cure aborts on generic q13, and a complete
  common-parent cure still gives a clean rank-3 q13 failure at `p≈4` after
  moving generation itself to a basis containing physical edge 13.
- Final UV status: **pass for the selected pristine remediation audit**.
  Masks 8 (q12), 14 (the q13 sector), and 15 (overall) were profiled on graph
  0/LMB 0 at five random points over exponents 6..10 for all 256 orientations.
  Every sample was finite; every fitted orientation passed and every missing
  fit was classified as vanishing.
- Integration converged: **yes for the final pristine graph** under the
  campaign definition. The earlier 21,500-point no-directive integration is
  subtraction-equivalent to the final graph.
- No `V_36` or `V_37` vertex is present, so no hard-collinear sector is
  required.

Every GammaLoop invocation in this continuation used
`scripts/run_guarded.py`, the shared two-job lock, a 15 GiB recursive-RSS cap,
a 12 GiB disk floor, a 6 GiB host-memory reserve, the pinned release binary,
and `RAYON_NUM_THREADS=3`. No source, test, plan, or campaign summary was
changed.

## Topology and generated structure

The four-loop generation basis remains `[2,4,6,12]`, with

```text
q12 = K3
q13 = K1 - K2 - K3.
```

The two massless internal gluons therefore have rank-3 independent
single-soft limits and a rank-6 double-soft limit. GL086 has 256 orientations,
108 valid full LMBs, and three process-valid cuts:

| cut | edges | active left thresholds |
|---:|---|---|
| 0 | `[2,3,11,12]` | E42, E36, E37, E19 |
| 1 | `[2,3,9]` | E31, E36, E32, E37, E33* (pinched), E19 |
| 2 | `[2,3,6,12,13]` | none |

The active surfaces are E19=`[6,7]`, E31=`[9,13,14]`,
E32=`[7,9,12,13]`, E33=`[6,9,11,14]`, E36=`[7,11,13]`,
E37=`[6,12,14]`, and E42=`[11,12,13,14]`.

The final full-UV regeneration is exact for the pristine DOT and retains all
14 evaluators. It completed in 41.423256612 s internal time, used 675,479,552
bytes internal peak RAM, and compiled no code. Its external guard completed
normally in 42.275 s with 663,511,040 bytes recursive peak RSS.

## Baseline obstruction

The decisive path sets `q12=0` where E31/E32/E36/E42 coalesce. With generated
default threshold subtraction, the negative branch has `p=3.948..3.983` and
the positive branch `p=6.135..6.507`, all with good R2. The identical
threshold-off diagnostic has `p=0.951..0.996`. Thus the excessive suppression
is threshold-counterterm driven rather than inherited from the original LU
integrand.

Constant diagnostic multipliers then localized the bad residual. At
`|lambda|=1e-6`, cut-0 E36 and E42 were individually large and cancelled to
the residual. On cut 1, E31/E32 were negligible while E36 supplied the
relevant projected term. This motivated a complete cut-0 hard lift and a
minimal cut-1 E36 lift.

## Rejected q12-only cure

The strongest q12 prescription was

| cut | threshold(s) | subspace | parent LMB |
|---:|---|---|---|
| 0 | E19, E36, E37, E42 | `[6,11]` | `[3,6,11,12]` |
| 1 | E36 | `[6]` | `[3,6,9,11]` |

All other cut-1 associations retained their autogenerated defaults. The
generated registry proved subtraction completeness: ten active variants,
twenty local/integrated components (`component_id=0..19`), and 14 evaluators.
The decisive q12 asymptotic fit passed at
`p=1.999998..2.000008`, span `9.8e-6`, with near-unit R2.

All four relevant IR-away threshold paths completed with 100/100 finite
points, no skips, and a nonzero CT contribution at every point:

| surface | p envelope |
|---|---:|
| E31 | `[-0.0000413,0.0000413]` |
| E32 | `[-0.04491,0.07287]` |
| E36 | `[-8.31e-8,8.31e-8]` |
| E42 | `[-0.03134,0.01705]` |

For these nearly constant series the threshold-only classifier used
`minimum_r_squared=0`, because a finite `p=0` series has an uninformative
log-log R2. E36 required a genuinely generic exact threshold crossing,

```text
K1=(168.64647067053642,47,-163)
K2=(-137,83,56)
K3=(74,-119,91),  dK1x=1,
```

where `|q12|=167.09` GeV and `|q13|=395.79` GeV. This replaced an aligned
radial-scaling probe whose geometry—not its surface—caused a center failure.

Despite that strong local evidence, the complete final-state inventory exposed
a fatal omitted region: the generic `q13 -> 0` path aborts on cut-1 projected
instance 1, registry variant E31, in overlap group 0. The q12-only prescription
and its in-progress UV profile were therefore stopped and preserved as
`state_GL086_minimal_q12_rejected`; they are not the final graph.

## Rejected explicit-q13 common-parent cure

The final structural attempt made q13 explicit while obeying the rule that all
threshold subspaces on one cut side share a parent. Cut 0 kept the four
`[6,11]` projections above. All six cut-1 associations moved to the
topology-valid parent `[3,6,9,13]`:

- E19/E32/E33/E37 used the two-loop subspace `[6,13]`;
- E31 used the physical e13 one-loop subspace `[13]`;
- E36 used the one-loop subspace `[6]`.

Generation retained 14 evaluators, so no threshold association disappeared.
This prescription removed the q13 radial-center abort, but it did not cure the
limit. The rank-3 generic q13 fit is the smooth, decisive failure

```text
p = 4.000030 .. 4.000473,  R2 > 0.9999999988.
```

Its q12 bridge also degraded to an inconclusive `p=1.533..1.795` with
R2 `0.984..0.992`. With no credible path to pass both gates inside the lease,
the candidate was rejected before threshold-only and UV work. It is preserved
as `state_GL086_common37_rejected`.

Earlier rejected diagnostics are also retained: common `[13]` prescriptions
with parent 99 and cut-native parents, a cut-native `[6]` cycle prescription,
a cut-0 E36/E42-only lift that merely exposed E19/E37, and the over-broad
cut-1 E31/E32/E36 `[6]` prescription whose E31/E32 threshold rays had invalid
radial centers. This common-parent attempt did not establish basis invariance:
although its counterterm parent contained physical edge 13, generation still
used pristine basis `[2,4,6,12]`. The continuation below therefore performed
the genuine generation-basis move before accepting the `p≈4` obstruction as
decisive.

## Genuine remapped-basis no-go

All 108 topology-valid full LMBs were enumerated before the edit.
`[3,6,9,13]` is valid and exposes q13 as an independent generated coordinate.
Writing the pristine generated basis as `K=(e2,e4,e6,e12)` and the candidate
as `J=(e3,e6,e9,e13)`, momentum conservation gives the shift-free map

~~~text
J = (K1-K0, K2, K1, K1-K2-K3)
K = (J2-J0, J2, J1, J2-J1-J3)
q13 = J3
q12 = J2-J1-J3.
~~~

Every stored approach point and axis was transformed with this exact map. The
candidate used one complete subtraction family, with neither multipliers nor
disabled associations:

| cut | threshold(s) | subspace | common parent LMB |
|---:|---|---|---|
| 0 | E19, E36, E37, E42 | `[6,11]` | `[3,6,11,12]` |
| 1 | E19, E32, E33, E37 | `[6,13]` | `[3,6,9,13]` |
| 1 | E31 | `[13]` | `[3,6,9,13]` |
| 1 | E36 | `[6]` | `[3,6,9,13]` |

This implements the `ttH_defo.pdf` strategy: use the shared one-loop
subspace for E36 and introduce the second loop only where physical edge 13 is
genuinely separate, while keeping one parent across the cut side so
cross-threshold soft cancellations are not fragmented. The generated registry
contains ten active variants and twenty local/integrated components. All
requested subspaces and parents resolve exactly, all variants are generated
and active, and generation retains all 14 evaluators.

The clean full generation completed in 41.437077749 s internal time, with
757,456,896 bytes internal peak RAM, 4.369479508 s in Spenso, 14.714636616 s
in Symbolica, and no compilation. Its guard completed in 42.305871964 s with
657,166,336 bytes recursive peak RSS and at least 428,712,787,968 bytes free
on disk. The three-gate guard completed normally in 71.729446411 s with
677,830,656 bytes peak RSS and at least 428,610,289,664 bytes free; each
approach evaluated 100/100 points with no skips.

The generic q12 limit passes:

~~~text
p = 1.0000814238354838 .. 1.0040509162693079
span = 0.0039694924338241, minimum R2 = 0.999998439921789.
~~~

The native q13=`J3` limit is nevertheless a smooth, decisive failure:

~~~text
negative: p = 4.000303054157231, 4.000472723478304, 4.000756207679872
positive: p = 4.000030133850175, 4.000047018582129, 4.000075254408817
minimum R2 = 0.999999995035619.
~~~

The former E31/E32/E36/E42 root blocker also evaluates completely, but its
fits are too noisy to classify (`p=2.6317245566841163..7.635017427251754`,
`R2=0.3015..0.4535`). The native q13 gate alone is decisive and reproduces
the old `p≈4` behavior in a genuinely different generation basis. The
candidate was therefore rejected before candidate UV or integration, no
further prescription was cycled, and the pristine DOT was restored
byte-for-byte.

## Selected pristine UV audit

The final DOT was already pristine when the selected UV study ran. Threshold
subtraction remained enabled, while event generation and additional weights
were disabled. For graph 0/LMB 0, each selected mask used five random points
at exponents 6..10 and all 256 orientations:

| mask | physical target | fitted orientations | vanishing orientations | result |
|---:|---|---:|---:|---|
| 8 | q12 single-soft | 184 | 72 | pass |
| 14 | three-coordinate q13 sector | 0 | 256 | pass (vanishing) |
| 15 | overall four-loop scaling | 0 | 256 | pass (vanishing) |

Every orientation in every profile has five finite samples. For mask 8, the
fitted-orientation slope envelope is
`[-1.0001054640052098,-0.999963099906272]` with minimum
`R2=0.9999999918562104`. The orientation sum has
`slope=-1.0000035444810056` and `R2=0.9999999999907974`. Its remaining 72
orientations satisfy the profile's vanishing contract. For masks 14 and 15,
all 256 orientations have zero positive samples and are explicitly marked as
vanishing, with no bad or ambiguous entry.

The combined guard completed normally in 111.400212765 s, peaked at
388,931,584 bytes recursive RSS, and kept at least 428,722,749,440 bytes of
disk free. The earlier exhaustive 33-point/all-subset attempt remains honestly
recorded as incomplete: it timed out without a JSON artifact. It is superseded
for final classification by this tailored high-risk-sector audit, not
retroactively reclassified.

- Mask-8 profile SHA-256:
  `9545c4c6bda226da5c78f39c5981fabaf2761e5b44670ad10e0ce2163e8a728d`.
- Mask-14 profile SHA-256:
  `1038f2cd1ac62c81cf247380a18cab4776e70ef9dbac67f4755634158524fe15`.
- Mask-15 profile SHA-256:
  `02a530b5ad42aa5e452308e3bc683953c70b27abb2ad9626b2fdf89a88caa839`.
- Combined UV guard SHA-256:
  `3ba9b72e258ef44ed502dc57fc78a447238773a4a08191f1bd58391c5c8c634a`.

## Final pristine state and provenance

Both common-parent candidates were quarantined and the temporary remapped
coordinates were reversed. The final DOT is byte-identical to the pristine
graph, has exactly four `lmb_id` edges, and has no `pin`, `dir`, `lmb_rep`,
or `threshold_counterterms` attribute. The 21,500-point integration is
therefore carried forward without rerunning it.

- GammaLoop CLI SHA-256 for this continuation:
  `0f8ed2c842814995bdcad19b3bc2a406eb75cb4a1784a74aa3c3de6b0e9d2b1b`.
- Historical final-state generation CLI SHA-256:
  `d655c43b3ea32797095c8f8d787176581c843b62840ae0af29337ddfacaff9b4`.
- Final DOT SHA-256:
  `1b5effa9ab2d81783a5b6eb7e44bfe5f654473de6f57be63baaecdd7f32e6cb9`.
- Final run-card SHA-256:
  `ec5b976f6f7e6952e7b6a1c4f969cceb79444e24b9149edd7a4712506fdd5770`.
- Final `cs.bin` SHA-256:
  `6e6354d1bf59580d550463767a4aaceba9bc39facbb6196c8d074860f1280283`.
- Final `integrand.bin` SHA-256:
  `e510ef3d2e4d2001aef22c0d8914aa09b0cd4c34ecb927da966e17b329038edd`.
- Final generation-summary SHA-256:
  `725da9280f394b73200d2c8ac09b1ccc1a4d338072224079b1c604bf704b76ee`.
- Rejected remapped-candidate generation-summary SHA-256:
  `acb1fbef2cffb0754411e1f9308ba3d2b5a3f53ce8dcfc6266c842b6eb1b8de7`.

The existing pristine integration reached 21,500 samples with 0% nonfinite or
unstable points. Its componentwise-absolute relative errors are 53.24% (real)
and 45.30% (imaginary), both below the campaign's 75% cutoff; it remains
converged for the byte-identical final no-directive graph.

Conclusion: GL086 remains **unresolved**. A complete,
subtraction-preserving q12 cure exists, but a complete physical-edge-13
family has a clean non-integrable threshold-driven `p≈4` q13 limit in both
the pristine and genuinely remapped generation bases. No directive is
accepted.
