# GL136

## Provenance and topology

- Audit: one 30-minute graph lease on 2026-08-09; no GammaLoop source edit or build.
- GammaLoop CLI SHA-256: `fc9d20e0ffbca1b334ab57908b29aeea34ecda68f5c3a76664d8ca4f8536256c`.
- Raw diagnostic DOT SHA-256: `e5b371946dd28c392a86801f04d328d48b37c18d3b296db5bddff565ae1fa663`.
- Final clean DOT SHA-256: `cee7a8950ca481c611ccf365702a23fe883aacc92a6cf311365c67e072bb2e16`.
- Final run-card SHA-256: `cc9dc9ee021f27a531826ffbe7faeb3b71a3418588b5b44f4a87f0df118236eb`.
- Generation-summary SHA-256: `77321a98991a8e03cb0360142b895c9cf5500be2f322f1b6e2489bd59fb710b7`.
- Integration-result SHA-256: `6585b0533b6bb5b474f89f9ee2c31670def0dc663e2bad4dcf5418459aabe47b`.
- The four-loop basis is e2/e4/e6/e13. The clean DOT preserves exactly those four `lmb_id` fields and contains none of `pin`, `dir`, `lmb_rep`, or `threshold_counterterms`.
- The only internal gluons are e12 and e13. There is no `V_36` or `V_37`, so no hard gluon-collinear sector is required.

The freshly read raw momentum routing, with spatial external momentum sum zero, is

```text
q2=K0, q3=-K0+K1, q4=q5=K1, q6=K2,
q7=q9=K2+P0+P1, q8=-(P0+P1), q10=P0+P1,
q11=K1+K3, q12=-K2-P0-P1+K1+K3,
q13=K3, q14=-K3+K2+P0+P1.
```

The independent spatial IR variables are therefore `q13=K3` and `q12=K1+K3-K2`. This routing was derived directly for GL136; its threshold inventory below was not inferred from GL134.

## Generated cut and threshold structure

Full local and integrated UV generation succeeded with the exact ttH NNLO process specification, all **64 orientations**, and the sole process-valid cut

```text
cut 0 = (2,3,6,12,13)
```

No cut or orientation filter is present. The generated E-surfaces are E3=`(6,9)`, E4=`(2,3,6,11,14)`, E5=`(6,13,14)`, E6=`(6,7)`, E9=`(4,6,11,14)`, E11=`(5,6,11,14)`, E30=`(6,11,12)`, E31=`(5,6,12,13)`, E32=`(2,3,6,12,13)`, and E33=`(4,6,12,13)`. **None is active in cut 0.** Consequently no threshold counterterm can spoil the relevant LU cancellation, there is no correlated soft/threshold family to add, and no threshold-only check is applicable to an edit.

Generation produced two evaluators, a 4.95 MiB integrand binary, no compiled shared object, and zero compilation time. The authoritative internal generation time is **1.141165416 s**, with internal peak RAM **146,587,648 bytes**. The guarded wrapper completed in 1.580 s, peaked at 159,350,784 bytes, and observed at least 132,489,048,064 bytes free disk.

## Reproducible IR paths

Every approach uses the complete graph sum with no orientation ID, `--momentum-space --graph-id 0`, 50 points on each signed logarithmic branch from `1e-6` to `1e-2`, the midpoint skipped, and three cores. Threshold subtraction remains enabled throughout.

| limit | midpoint `(K0;K1;K2;K3)` | axis `(dK0;dK1;dK2;dK3)` | rank |
|---|---|---|---:|
| soft e13 | `(37,126,-109; -91,64,83; 31,-47,59; 0,0,0)` | `(0;0;0; 211,-157,193)` | 3 |
| soft e12 | `(37,126,-109; -91,64,83; -60,17,142; 31,-47,59)` | `(0;0; 211,-157,193;0)` | 3 |
| independent double soft | `(37,126,-109; -91,64,83; -91,64,83; 0,0,0)` | `(0;0; 113,-71,-149; 211,-157,193)` | 6 |

For soft e13, `q13=lambda*(211,-157,193)` while q12 has a nonzero hard midpoint. For soft e12, `q12=-lambda*(211,-157,193)` while q13 remains hard. On the double-soft path, `q13=lambda*(211,-157,193)` and `q12=lambda*(98,-86,342)`, which are independent. Every target norm therefore has exact log-log slope one on both branches; all generic complements remain nonzero, and no unlisted IR or active-threshold locus is approached.

## Fits and directive diagnosis

The entries below give `p` for the closest 8/12/16 finite nonzero points on each branch. All complex-norm fits satisfy `R2 >= 0.995`, envelope span at most 0.5, and branch agreement.

| limit | negative-branch p (8/12/16) | positive-branch p (8/12/16) | complete p envelope | minimum R2 | result |
|---|---|---|---|---:|---|
| soft e13, r=3 | `1.000017 / 1.000026 / 1.000042` | `1.000001 / 1.000002 / 1.000003` | `[1.000001,1.000042]` | 0.999999999753 | pass |
| soft e12, r=3 | `1.000019 / 1.000030 / 1.000048` | `1.000001 / 1.000001 / 1.000002` | `[1.000001,1.000048]` | 0.999999999677 | pass |
| double soft, r=6 | `2.000020 / 2.000032 / 2.000051` | `2.000015 / 2.000023 / 2.000037` | `[2.000015,2.000051]` | 0.999999999911 | pass |

At the closest negative-branch points, `(total, original)` norms are respectively `(7.85082e-36, 7.85082e-36)`, `(2.88422e-36, 2.88422e-36)`, and `(1.10934e-29, 1.10934e-29)`. Every threshold-counterterm sum is absent, matching the cut metadata. All required limits pass, and **no threshold-counterterm directive is edited or justified**.

## Integration and extrema

- UV mode: full local plus integrated UV.
- The planned watchdog interrupt retained iteration 4346, for **434,600 samples**.
- Final integration-log runtime: **1.24 ms/sample/core** on three cores (the JSON average total time, 0.953968 ms, is only a cross-check).
- Signed result: `(-5.923439078935128e-10, +7.991848310696292e-27 i)` with errors `(3.636805674444157e-11, 7.155101896690959e-27)`.
- Componentwise-absolute result: `(+8.364208483089583e-10, +8.474571965640433e-26 i)` with errors `(3.6357023212684406e-11, 7.153957290239415e-27)`.
- Absolute relative errors: **4.346738%** for `|re|` and **8.441674%** for `|im|`.
- Stability: f64/f128/Arb `100%/0%/0%`; nonfinite and retained finite-Double unstable shares are both **0%**.
- `converged? = yes`: the checkpoint is finite, contains no nonfinite samples, and both nonzero absolute components are below the 75% relative-error cutoff. The signed imaginary relative error (89.5%) is reported but is not used for convergence.
- Guarded integration duration: 212.509 s including checkpoint grace; peak recursive RSS 308,117,504 bytes; minimum free disk 132,356,591,616 bytes; no child survived.

Signed maxima are re+ `+4.0214349491743251e-6`, re- `-6.9714048656167408e-6`, im+ `+1.8666469114841083e-21`, and im- `-8.0375016190559458e-22`; the componentwise-absolute maxima reuse re- and im+. All four unique coordinates are preserved verbatim in `inspect_max_weights` in the final card. They were **not replayed** because the coordinator froze further scientific runs after the passing IR evidence and completed integration. This is explicitly an incomplete extremum diagnostic, not a classification of those points.

## Conclusion

GL136 has ten enumerated threshold surfaces but none is active in its sole process-valid cut. Its two independent single-soft limits and double-soft intersection all pass by wide integrability margins, so `all limits OK? = yes` and no directive is needed. The full-UV three-minute diagnostic converged with 434,600 finite, stable samples. Only max-weight replay remains deliberately unperformed under the coordinator freeze.
