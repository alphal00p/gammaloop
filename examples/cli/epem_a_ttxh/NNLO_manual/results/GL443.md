# GL443 threshold/IR audit

## Remediation continuation (2026-08-09; superseding the audit below)

This continuation supersedes the historical status, directive decision,
generation timing, UV status, and conclusion retained below for provenance.

### Final status and provenance

- **Threshold structure edited:** no. Every tested directive and generation-
  basis move was rejected, and the final DOT is byte-for-byte pristine with
  SHA-256 `157ab0c8f97bdb24210e107b31aa1d92a344749d5d6f8eab45cc1f20bd19f6ea`.
- **All limits OK:** no. The best projected prescription restores the generic
  q11 soft law but the mandatory q11+E20/E43/E31/E39 path still stops on the
  physical-cut-2 E23 counterterm before producing JSON.
- **Converged:** no final-candidate integration was rerun. The historical
  100-sample checkpoint below is retained as diagnostic evidence, but it is
  not promoted because the same threshold-root blocker terminated that run.
- Final run-card SHA-256:
  `26a377b147d1398b8fe2a687dd33e8679451d4c0fcb873682815a4c7beac9f99`.
- Pinned CLI SHA-256:
  `0f8ed2c842814995bdcad19b3bc2a406eb75cb4a1784a74aa3c3de6b0e9d2b1b`.
- Every GammaLoop invocation used `scripts/run_guarded.py`, the shared two-job
  lock, a 15 GiB recursive-RSS cap, 12 GiB disk floor, 6 GiB available-memory
  floor, and `RAYON_NUM_THREADS=3`. No source change, epsilon regulator,
  selected-orientation shortcut, disabled association, or threshold-off cure
  was used.

### Causal diagnosis and complete inventory

The pristine graph retains 464 orientations, six physical cuts, 23 evaluators,
and all 16 active cut/threshold associations listed in the historical audit.
A tagged diagnostic generated exactly 32 active detailed components: one local
and one localized-integrated component for every association, each with
`occurrence_count=1` and `evaluation_skipped=false`. This corrected the blocker
attribution without changing any CT geometry.

At q11=0 the active thresholds coalesce into
E22/E35/E23/E34 and E20/E43/E31/E39. The identical generic q11 path with
threshold subtraction disabled is clean, with
`p=1.000003..1.000037` and minimum R2 `0.9999999998`; threshold-on pristine
data are cancellation-limited and noisy. The threshold-off copy of the
mandatory second-family path evaluates all 100 signed points, but its total
weight and the original cut-2/cut-5 pieces are nonfinite/null because that
threshold subtraction is required. Together these tests isolate the CT
construction as the cause while ruling out subtraction removal as a cure.

Schema-v3 variant registries localize the pristine mandatory error to internal
cut group 0, physical cut 2. Its first surface is E22 `(8,11,12)`. Projecting
E22 makes that instance valid, after which the next surface, E23 `(7,8)`, is
the stable repeated blocker. E23 fails center/radial-root validation as the
default `[7,11]` copy, as one-loop `[7]` or `[8]` copies in three distinct
parents, and as a full two-loop `[8,13]` copy.

### Threshold-structure and basis candidate ledger

Every successful candidate generation retained all 23 evaluators and all 16
active variants. Rejected partial states and guard records are preserved under
`states/state_GL443_candidate_*` and `states/GL443_guard`.

| candidate | generic q11 result | decisive outcome |
|---|---|---|
| targeted E43/E39 and E20/E31 one-loop copies | q14 remains `p=2.000012..2.000070` | mandatory path still aborts pristine E22 |
| q14 shared-parent attempts | not reached | mixed parent LMBs 59/80 reject generation |
| complete q14 parent rebase | `p=1.000005..1.000041`, pass | mandatory path still aborts E22 |
| complete same-cut shared one-loop routing | `p=0.999925..1.000010`, pass | advances to projected E23 instance 1, then aborts |
| all E22/E35/E23/E34 copies in legacy `[7]` | noisy `p=3.088071..4.903478` | E34 common-center construction aborts |
| only embedded E22/E35 in `[7]` | `p=1.999812..1.999956`, pass | advances to default E23, then aborts |
| E22/E35/E23 in `[7]`, E34 left full | `p=1.999958..2.000013`, pass | projected E23 still aborts |
| coherent hard-cycle parents `[4,5,7,12]` / `[2,6,7,12]` | `p=2.999955..3.000034`, inconclusive | projected E23 still aborts |
| complete routing with E23 restored to full two loops | `p=2.999962..3.000049`, inconclusive | full-space E23 still aborts |
| default CTs in cut-2-aligned generation basis `(2,4,8,13)` | noisy `p=3.273969..4.507928` | pristine E22 still aborts |
| complete routing in basis `(2,4,8,13)` | `p=0.999933..1.000011`, pass | projected E23 still aborts |

The basis move used the exact spatial map
`K'=(K0,K1,K2,K2-K3)` for every generic, correlated, q14, and double-soft
point and axis. It therefore compares the same physical limits and does not
hide the blocker by changing kinematics. The original basis and every original
coordinate were restored after rejection.

The data establish a sharp tradeoff: the complete one-loop routing is the only
tested structure with the expected generic q11 cancellation, but its E23 copy
cannot be evaluated on the other mandatory threshold family. Leaving E23 in a
larger space either retains the same root failure or produces an uncancelled
`p≈3` law. A GL638-style smooth eta partition cannot repair an invalid nonzero
variant because every nonzero copy is evaluated before it can contribute;
piecewise theta selection is not available in the tested interface, and a late
exact-zero gate is forbidden by the campaign plan. No directive is therefore
scientifically admissible.

### Final full-UV generation and UV certification

The pristine graph was regenerated from a clean state with full local and
integrated UV subtraction. `generation_summary.json` records 23 evaluators,
`stats.total_time=206.184380579 s`, including `15.436193786 s` in Spenso and
`159.069719531 s` in Symbolica, zero compile time, and peak RAM
`3,019,862,016` bytes. The guard completed in `207.571502 s`, with peak
recursive RSS `2,899,955,712` bytes and minimum free disk
`428,835,340,288` bytes.

The final seeded-random profile uses seed `44320260809`, graph/LMB `0/0`, five
points at scales `1e4..1e8`, all 464 orientations, and two two-loop masks. It
contains exactly 928 orientation entries and every entry has five finite
samples; no sample is NaN.

| mask | free loops | fitted orientations | exact-vanishing orientations | slope range | min R2 | result |
|---:|---|---:|---:|---:|---:|---|
| 12 | `(K2,K3)` / `(e7,e11)` | 336 | 128 | `[-1.077064,-0.991512]` | `0.9967716` | pass |
| 10 | `(K1,K3)` / `(e6,e11)` | 0 | 464 | n/a | n/a | pass as exact vanishing |

For mask 12 the summed inspect fit is `-0.9999515895` with
R2 `0.9999999983`, estimated DOD `-1`, and an Arb retry. Exactly one finite
Double tail sample is marked unstable (orientation `++++-+--+00-+-+`, scale
`1e8`, estimated relative accuracy `7.48e-5`); its five-point fit remains
`-1.077064` with R2 `0.9967719`. All other fitted records are stable. Mask 10
has five exact-zero samples for every orientation and
`missing_fit_is_vanishing=true`, consistent with its analytic DOD `-2`.
The profile SHA-256 is
`4fc3406f6e7a596eaf50699b605cc24593e81251c3c9c42a8277105f51e4cbd5`.

### Remediation conclusion

`threshold struct. edited? = no`; `converged? = no` for final-candidate
evidence; `all limits OK? = no`. Full UV generation and the two mandatory
orientation-resolved UV sectors are safe. GL443 remains unresolved solely
because the q11 subtraction needs the shared one-loop E23 behavior for LU
cancellation while the current center/root machinery cannot construct that
copy on the E20/E43/E31/E39 family. The pristine DOT is deliberately retained
until branch-selective projected CTs, or a correction to E23 existence/root
handling, can express both requirements without removing threshold subtraction.

---

## Historical audit retained for provenance

## Provenance and generation

- Raw diagnostic DOT SHA-256: `72bd7b60670434ec6921cb5229493ad51fa7c5f4794742c73106ca50c997ff65`.
- Clean committed DOT SHA-256: `157ab0c8f97bdb24210e107b31aa1d92a344749d5d6f8eab45cc1f20bd19f6ea`.
- Final run-card SHA-256: `260a925c658f31fe0da61bc10612ee6b2e69663d1c1af78ac14e397962ef3784`.
- Pinned CLI SHA-256: `fc9d20e0ffbca1b334ab57908b29aeea34ecda68f5c3a76664d8ca4f8536256c`.
- Full local and integrated UV generation succeeded; no fallback was used.
  `generation_summary.json` reports `stats.total_time = 99.801252209 s`,
  23 evaluators, four generation cores, 3,780,935,680 bytes peak RAM, and
  no compilation.
- The exact ttH NNLO process specification retained all 464 orientations and
  all six process-valid cuts. The card has no cut/orientation filter or
  orientation ID.

| cut | edges | left thresholds | right thresholds |
|---:|---|---|---|
| 0 | `(2,5,12,14)` | E22, E23 | none |
| 1 | `(2,5,7,11,14)` | none | none |
| 2 | `(2,4,5)` | E34, E22, E35, E23, E43, E39 | none |
| 3 | `(2,3,13,14)` | E35, E23 | none |
| 4 | `(2,3,8,11,14)` | none | none |
| 5 | `(2,3,6)` | E34, E22, E35, E23, E20, E31 | none |

The generated E-surfaces are E14 `(3,5)`, E15 `(2,4,5)`,
E16 `(2,5,12,14)`, E18 `(2,3,6)`, E19 `(4,6)`, E20 `(6,12,14)`,
E22 `(8,11,12)`, E23 `(7,8)`, E29 `(2,5,7,11,14)`,
E31 `(6,7,11,14)`, E34 `(12,13)`, E35 `(7,11,13)`,
E38 `(2,3,8,11,14)`, E39 `(4,8,11,14)`, E42 `(2,3,13,14)`, and
E43 `(4,13,14)`. Active instances are E20/E22/E23/E31/E34/E35/E39/E43.

## Routing and singular loci

GL443 has four loops, no `V_36` or `V_37`, and no applicable hard-collinear
sector. With zero incoming spatial momentum sum, the raw routing is

```text
e2  = K0              H, lmb_id 0
e3  = K1-K0           t
e4  = K1              t
e5  = K1-K0           t
e6  = K1              t, lmb_id 1
e7  = K2              t, lmb_id 2
e8  = K2              t
e11 = K3              g, lmb_id 3
e12 = K2-K3           t
e13 = K2-K3           t
e14 = K1+K3-K2        g
```

Thus `q11=K3` and `q14=K1+K3-K2` are independent rank-3 soft
constraints; their common locus `K3=0, K1=K2` has rank 6. E20/E43,
E22/E35, and E31/E39 are distinct E-surface instances with identical
kinematic equations before any limit.

At q11 soft the groups are E22/E35/E23/E34 and E20/E43/E31/E39. At q14
soft they are E22/E35/E31/E39 and E20/E43/E34. At double soft all eight
active instances coalesce into one radial equation.

For `Et(k)=sqrt(|k|^2+173^2)`, the distinct active distances are

```text
eta20 = eta43 = Et(K1)+Et(K2-K3)+|q14|-1000
eta22 = eta35 = Et(K2)+|K3|+Et(K2-K3)-1000
eta23         = 2 Et(K2)-1000
eta31 = eta39 = Et(K1)+Et(K2)+|K3|+|q14|-1000
eta34         = 2 Et(K2-K3)-1000.
```

## Explicit all-orientation evidence

Each block uses the complete graph sum, 50 logarithmic points per signed
branch, `1e-6 <= |lambda| <= 1e-2`, skipped midpoint, and three cores.
Coordinates are `(K0;K1;K2;K3)`. All produced JSON files contain an active
threshold-counterterm contribution. All finite target-distance slopes lie in
`[0.999997276,1.000017873]`.

| path (rank) | midpoint | axis | p envelope | min R2 | result |
|---|---|---|---:|---:|---|
| e11 soft (3) | `(37,126,-109;74,-52,137;-91,64,83;0)` | `(0;0;0;211,-157,193)` | `[3.253346,5.160555]` | 0.786896 | inconclusive |
| e14 soft (3) | `(37,126,-109;74,-52,137;105,-99,196;31,-47,59)` | `(0;98,-86,342;0;0)` | `[2.000012,2.000069]` | 1.000000 | pass |
| double soft (6) | `(37,126,-109;74,-52,137;74,-52,137;0)` | `(0;0;113,-71,-149;211,-157,193)` | `[2.875502,5.244180]` | 0.820510 | inconclusive |
| e11 + E22/E35/E23/E34 (3) | `(37,126,-109;74,-52,137;0,0,469.117256;0)` | `(0;0;0,0,23;211,-157,193)` | `[3.512464,4.063101]` | 0.818399 | inconclusive |
| e14 + E22/E35/E31/E39 (3) | `(37,126,-109;0,0,364.966128;0,0,464.966128;0,0,100)` | `(0;98,-86,365;0,0,23;0)` | `[1.999974,2.000099]` | 1.000000 | pass |
| e14 + E20/E43/E34 (3) | `(37,126,-109;0,0,469.117256;0,0,569.117256;0,0,100)` | `(0;0,0,23;-98,86,-319;0)` | `[2.000004,2.000067]` | 1.000000 | pass |
| double + all active E-surfaces (6) | `(37,126,-109;0,0,469.117256;0,0,469.117256;0)` | `(0;0,0,23;113,-71,-126;211,-157,193)` | `[3.186218,7.116897]` | 0.687305 | inconclusive |

The remaining mandatory q11+E20/E43/E31/E39 block is explicit in
`all_approaches` but produces no JSON. It reaches final f64 and aborts because
cut group 0, left overlap group 0, E-surface 0 fails center/radial-root
validation in the identity rotation. The high-p q11 fits above are formally
inconclusive because of poor R2, broad envelopes, or both. No path is a smooth
branch-consistent formal failure, so neither threshold-off nor Arb is
authorized by the precision gate.

## Directive decision

No `threshold_counterterms` directive is accepted. The q14 families pass, but
q11 and double-soft evidence remains inconclusive and one central q11
intersection is unevaluable. Guessing a projected parent would violate the
evidence requirement. Threshold subtraction remains enabled. With no DOT edit,
no threshold-only post-edit path is required.

## Integration

The guarded integration retained one completed iteration (100 samples) before
the next sample hit a fatal threshold-CT validation error: cut group 0, left
overlap group 0, E-surface 2 failed center/radial-root validation in the
identity rotation. The guard reports return code 0 and
`exit_reason=completed` after 6.885 s because the current CLI returns success
after printing this error; the GammaLoop log is authoritative. Peak recursive
RSS was 5,941,231,616 bytes, minimum free disk was 123,842,375,680 bytes, and
the completed status reports 72.8 ms/sample/core.

| quantity | real | imaginary |
|---|---:|---:|
| signed central value | `1.8404737878e-6` | `3.2684923082e-6` |
| signed error | `1.7245684256e-6` | `2.8299933688e-6` |
| componentwise absolute central value | `2.4392091447e-6` | `4.6974854409e-6` |
| componentwise absolute error | `1.7170478885e-6` | `2.8096048408e-6` |
| absolute relative error | 70.3936% | 59.8108% |

The checkpoint has zero nonfinite and zero unstable samples, with
f64/f128/Arb usage 100%/0%/0%. It mechanically satisfies the campaign
convergence rule because both absolute relative errors are at most 75%, but the
integration is scientifically incomplete because it terminated on the CT
validation blocker instead of reaching the three-minute timeout.

Persisted signed extrema are real `+1.6861303717e-4` (channel 2) and
`-1.7741589261e-5` (channel 5), and imaginary `+2.6408497894e-4`
(channel 3) and `-6.7838740569e-5` (channel 2). The componentwise-absolute
maxima are `1.6861303717e-4` and `2.6408497894e-4`. Their complete coordinates
are stored in `result_record.json`. They were not replayed after the science
freeze, so their IR/threshold-locus classification remains explicitly
incomplete.

## Conclusion

`threshold struct. edited? = no`; `converged? = yes` for the retained finite
checkpoint; `all limits OK? = inconclusive`. The q14 soft and correlated
families pass. The q11 and double-soft fits are formally inconclusive, one
mandatory q11 intersection cannot be evaluated, and the same class of CT
radial-root failure terminates integration after 100 samples. No projected
directive is evidence-supported, so threshold subtraction remains active and
the DOT is left unedited with all unresolved evidence recorded.
