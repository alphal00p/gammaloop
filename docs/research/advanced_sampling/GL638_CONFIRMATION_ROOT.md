# GL638 confirmation: canonical physical root interruption

The first frozen confirmation attempt stopped on the ordinary optimized-LMB
baseline at seed 20011, before physical body evaluation. The failing owner was
canonical physical overlap preparation for cut group 3 at Arb1000. The attempt
returned exit code 1 after 813.544 s; all 35 saved-file hashes stayed unchanged.
This failure does not invalidate the separately completed seven-method screen,
but the attempted confirmation is incomplete and is not pooled. The subsequent
[endpoint repair](CANONICAL_LU_ENDPOINT_CERTIFICATION.md) passes source gates
and the exact captured-source Double/Arb physical replay.

| Run order | Catalogue | Seed | Requested samples | Outcome |
| --- | --- | ---: | ---: | --- |
| 1 | optimized_lmb | 10007 | 32,768 | Completed |
| 2 | cuts | 10007 | 32,768 | Completed |
| 3 | cuts_combined_joint | 10007 | 32,768 | Completed |
| 4 | optimized_lmb | 20011 | 32,768 | Canonical physical root error |

The eleven remaining runs were not started. The complete first-iteration
checkpoints of the three finished runs remain distinct; no partial average,
replacement draw or proposal reselection is reported. This is the original
five-seed fixed-count confirmation declaration, not a new statistics sample.

The retained `DidNotConverge` result reports scale `t≈0.0450096019182`,
derivative `15947.7515848` and residual `−2.866985836e−298`. Its two nearby
bracket endpoints and all native digits are retained in `results/pilots.json`.
These printed diagnostics alone do not establish an acceptable root or its
failure mechanism. Unlike the earlier foreign phase-space callback failure,
this path already uses the routed ray and existing radial-root diagnostic
wrapper. Exact-source capture and the subsequent directed original-equation
comparison identify an already represented usable endpoint; the separate repair
record preserves those measurements.

The failed workspace contains only its manifest and settings: the error
propagated before the first completed-iteration checkpoint and no exact Sample
was emitted there. A separate deterministic source reduction now reproduces
the complete original native error string at worker 3, local index 239,
global index 5156, discrete path `[0,5]`. The captured twelve cube values match
their binary64 bit patterns; the complete cumulative Sample weight is 6.
The following canonical bridge evaluation supplies twelve native generation
momenta for the original-equation endpoint test. It does not replace or repair
the failed physical root solve.

The reduction uses the original fresh grid, mixed seed and source ranges,
with persistent worker integrands. It stops after the exact error match:
1,844 sources were evaluated and later workers may evaluate none. This is not
a complete 32,768-point scan. Whole-process time is 121.452 s; the client's
internal scope is 108.699 s. Both input and state hash records remain unchanged.
Fourteen independent record/source checks pass. This interruption archive
preserves the original failure; the separate [repair record](CANONICAL_LU_ENDPOINT_CERTIFICATION.md)
now validates its correction. A restored complete confirmation remains pending.

The [69-record attempt archive](gl638_hosted_joint_gate/mc_confirmation_root/attempt1/artifact_hashes.json)
preserves all 55 original result files, three completed checkpoints, run/log/
resource records, immutable declaration and the separate exact-source capture.
Its [provenance](gl638_hosted_joint_gate/mc_confirmation_root/attempt1/provenance.json)
distinguishes the failed integration from the successful partial source search.
Byte-identical prior settings and reports share storage; no generated-process
state payload or executable is copied. The [maximum replay and attribution](GL638_SCREEN_MAXIMUM_REPLAY.md)
are separate evidence and do not replace this interrupted fixed confirmation.
